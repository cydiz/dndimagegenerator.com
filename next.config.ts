import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuration options
  // Note: Turbopack is disabled via environment variable in package.json
  // to fix Google Fonts loading issues in Next.js 16
};

export default nextConfig;
